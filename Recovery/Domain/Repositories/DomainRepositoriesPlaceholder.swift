import Foundation

/// Platzhalter für Domain-Repository-Protokolle.
///
/// Hier werden feature-spezifische Protokolle definiert
/// (z. B. `AddictionRepository: Repository`), die von der Data-Schicht
/// implementiert werden.
enum DomainRepositoriesPlaceholder { }
